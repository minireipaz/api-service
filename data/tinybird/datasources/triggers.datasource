DATASOURCE triggers
ENGINE "MergeTree"
ENGINE_SORTING_KEY "id, workflow_id"

SCHEMA >
    `id` String,
    `workflow_id` String,
    `app_integration_id` String,
    `trigger_type` String,
    `trigger_config` String,
    `created_at` DateTime,
    `updated_at` DateTime
